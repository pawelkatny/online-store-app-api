const { User } = require('../models/user');

class AuthService {
    constructor() {
        this.User = User;
    }

    async register(userData) {
        
    }

    async login(loginData) {

    }

}